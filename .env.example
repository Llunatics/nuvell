# Environment Configuration for nuvelll — Indonesia Book Release Intelligence Platform

# Database Connection (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/nuvelll?schema=public"

# Redis Connection (for BullMQ Background Jobs & Cache)
REDIS_URL="redis://localhost:6379"

# Base Application URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Admin Authorization for /admin Crawler Dashboard
# Option 1 (Recommended): Comma-separated list of Google/Firebase emails authorized as admins
ADMIN_EMAILS="owner@example.com"
NEXT_PUBLIC_ADMIN_EMAILS="owner@example.com"

# Option 2 (Optional): Private secret string for server-to-server or emergency API access (generate a random string, never commit)
ADMIN_SECRET="your-private-random-secret-key"

# Crawler Polite Identification (Respectful User-Agent)
CRAWLER_USER_AGENT="nuvelll-bot/1.0 (+https://nuvelll.id/crawler-policy; contact@nuvelll.id)"

# Crawler Default Request Delay (in milliseconds, min 1500ms for respectful scraping)
CRAWLER_DEFAULT_DELAY_MS=2000

# Node Environment
NODE_ENV="development"

# Firebase Authentication & Cloud Firestore (Client SDK)
NEXT_PUBLIC_FIREBASE_API_KEY="your-api-key-here"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="nuvell-app.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="nuvell-app"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="nuvell-app.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="123456789012"
NEXT_PUBLIC_FIREBASE_APP_ID="1:123456789012:web:abcdef1234567890"
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="G-XXXXXXXXXX"
